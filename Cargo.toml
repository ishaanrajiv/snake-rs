[package]
name = "snake-rs"
version = "0.1.0"
edition = "2024"

[dependencies]
dirs = "5"
macroquad = { version = "0.4", features = ["audio"] }
quad-rand = "0.2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[patch.crates-io]
miniquad = { path = "vendor/miniquad" }
